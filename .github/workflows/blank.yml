name: CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: ls /usr/lib/jvm
    - run: echo $PATH
    - run: which java
    - run: realpath $(which java)
    - run: echo $JAVA_HOME
