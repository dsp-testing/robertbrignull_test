name: CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: ls /usr/lib/jvm
    - run: echo $PATH
    - run: which java
    - run: which java | realpath
    - run: echo $JAVA_HOME
